export class GenerateForm {
    constructor(
        bytes?: number,
        amount?: number,
        isHash:boolean = false,
    ) {
        if (typeof isHash === 'undefined'){
            isHash = false;
        }
     }
}