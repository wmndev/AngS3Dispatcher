"use strict";
var GenerateForm = (function () {
    function GenerateForm(bytes, amount, isHash) {
        if (isHash === void 0) { isHash = false; }
        if (typeof isHash === 'undefined') {
            isHash = false;
        }
    }
    return GenerateForm;
}());
exports.GenerateForm = GenerateForm;
//# sourceMappingURL=generate-form.js.map